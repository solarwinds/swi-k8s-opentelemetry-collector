# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: Test for gateway-statefulset
templates:
  - gateway/gateway-statefulset.yaml
  - gateway/gateway-config-map.yaml
  - common-env-config-map.yaml
chart:
  appVersion: 1.0.0
tests:
  - it: Gateway spec should match snapshot when using default values
    template: gateway/gateway-statefulset.yaml
    asserts:
      - matchSnapshot:
          path: spec.template.spec
  - it: Gateway collector should not be generated when it's disabled
    template: gateway/gateway-statefulset.yaml
    set:
      otel.gateway.enabled: false
    asserts:
      - hasDocuments:
          count: 0
