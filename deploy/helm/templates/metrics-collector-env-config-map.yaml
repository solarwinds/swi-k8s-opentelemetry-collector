{{- if .Values.otel.metrics.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}-metrics-env-config
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "common.labels" . | indent 4 }}
data:
{{- if .Values.otel.metrics.prometheus.autodeploy }}
  PROMETHEUS_URL: "prometheus-operated.{{ .Release.Namespace }}.svc.cluster.local:9090"
{{- else }}
  PROMETHEUS_URL: {{ quote .Values.otel.metrics.prometheus.url }}
{{- end }}
{{- end}}