receivers:
    nop:

processors:
    swmetricstransform:
        transforms:
          - include: old_name
            action: invalid # invalid action type

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [swmetricstransform]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [swmetricstransform]
            exporters: [nop]