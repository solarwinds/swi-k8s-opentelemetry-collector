receivers:
    nop:

processors:
    swmetricstransform:
        transforms:
            - include: name1
              metric_name: name2
              action: update

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [swmetricstransform]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [swmetricstransform]
            exporters: [nop]