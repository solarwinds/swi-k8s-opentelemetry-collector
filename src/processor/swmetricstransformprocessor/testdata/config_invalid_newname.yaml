receivers:
    nop:

processors:
    swmetricstransform:
        transforms:
            - include: old_name
              action: insert # missing new_name

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [swmetricstransform]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [swmetricstransform]
            exporters: [nop]