receivers:
    nop:

processors:
    swmetricstransform:
        transforms:
          - include: old_name
            action: combine
            new_name: new_name
            submatch_case: invalid

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [swmetricstransform]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [swmetricstransform]
            exporters: [nop]