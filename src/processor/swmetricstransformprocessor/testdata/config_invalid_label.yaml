receivers:
    nop:

processors:
    swmetricstransform:
        transforms:
            - include: old_name
              action: update
              operations:
                - action: update_label # missing label key

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [swmetricstransform]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [swmetricstransform]
            exporters: [nop]