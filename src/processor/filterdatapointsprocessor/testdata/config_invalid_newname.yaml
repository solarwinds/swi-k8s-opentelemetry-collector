receivers:
    nop:

processors:
    filterdatapoints:
        transforms:
            - include: old_name
              action: insert # missing new_name

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [filterdatapoints]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [filterdatapoints]
            exporters: [nop]