receivers:
    nop:

processors:
    filterdatapoints:
        transforms:
            - include: old[\da
              match_type: regexp
              action: update

exporters:
    nop:

service:
    pipelines:
        traces:
            receivers: [nop]
            processors: [filterdatapoints]
            exporters: [nop]
        metrics:
            receivers: [nop]
            processors: [filterdatapoints]
            exporters: [nop]